import GiftCard from '@/components/giftcard/giftcard';

export default function GiftLingon() {
  return (
    <div className="fixed inset-0 flex items-center justify-center">
      <GiftCard name="Lingon" />
    </div>
  );
}
